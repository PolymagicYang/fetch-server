# For documentation on how to configure this file,
# see diesel.rs/guides/configuring-diesel-cli

[print_schema]
file = "database/schema.rs"

import_types = ["diesel::sql_types::*", "diesel_geography::sql_types::*"]